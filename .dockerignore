node_modules
**/node_modules
**/dist
**/.next
**/coverage
.git
.env
.env.local
**/.env
**/.env.local

# Тот же кеш не должен попадать и в образ: даже не закоммиченный, он
# приедет из рабочей копии через `COPY . .` и сорвёт сборку так же тихо.
**/*.tsbuildinfo
