-- Личный вызов в «Горячо-холодно» существовал в БД (`targetUserId` на
-- HotColdDuel) с самого начала режима, но интерфейс им не пользовался —
-- зовущий мог лишь продиктовать код. Теперь это настоящий вызов другу, и
-- отказ от него, как и везде, должен доходить до отправителя.
ALTER TYPE "DeclineNoticeKind" ADD VALUE IF NOT EXISTS 'HOT_COLD_CHALLENGE';
