import * as THREE from 'three';
import { DIE_SIZE } from './die';

/**
 * Метры и одна общая система координат на всю сцену.
 *
 * Мера — настоящая: стол шириной 1,25 м, кость 3,2 см. Так свет,
 * глубина резкости и перспектива ведут себя предсказуемо, а «на глаз
 * побольше» не превращается в подбор чисел на каждом шаге.
 *
 * Начало координат — центр столешницы, `y = 0` — её поверхность,
 * `+z` — в сторону игрока, `−z` — к сопернику.
 */

export const TABLE = { width: 1.25, depth: 0.95, thickness: 0.05 };

/** Доска для костей: дно из сукна и деревянные бортики. */
export const BOARD = {
  centre: new THREE.Vector3(0, 0, -0.02),
  halfX: 0.17,
  halfZ: 0.13,
  floorTop: 0.012,
  rimHeight: 0.034,
  rimThickness: 0.02,
};

/** Высота центра кости, лежащей на сукне. */
export const DIE_REST_Y = BOARD.floorTop + DIE_SIZE / 2;

/**
 * Камера на уровне глаз сидящего человека.
 *
 * Наклон вниз — 20°, и это подобрано по кадру, а не на глаз. Голова
 * соперника сидит примерно на высоте глаз игрока, то есть **на линии
 * горизонта**, и в кадре она оказывается там же, куда попадает горизонт.
 * При наклоне в 24° горизонт уезжал в верхние 6 % экрана, и голова
 * пропадала за краем — на столе оставалось безголовое туловище.
 * _Нашлось живой проверкой, по снимку._
 *
 * Поднять камеру выше и смотреть круче вниз нельзя вовсе: это и есть
 * вид сверху, ради ухода от которого всё переделывалось.
 */
export const CAMERA = {
  position: new THREE.Vector3(0, 0.42, 0.62),
  target: new THREE.Vector3(0, 0.15, -0.13),
  /**
   * Угол задаётся **по горизонтали**, а вертикальный считается из него.
   *
   * У `PerspectiveCamera` угол вертикальный, и на экране телефона
   * (390×844, отношение 0,46) вертикальные 55° давали по горизонтали
   * всего 27°: телеобъектив, в котором соперник занимал три четверти
   * ширины, а доска упиралась в края. От горизонтали кадр ведёт себя
   * одинаково и на узком телефоне, и на планшете.
   */
  horizontalFov: 36,
  minFov: 45,
  maxFov: 78,
};

/** Соперник сидит напротив, за дальним краем стола. */
export const RIVAL = { z: -0.66, headY: 0.4 };

/** Где стоит кубок, откуда сыплются кости и куда он возвращается. */
export const CUP_SPOTS = {
  you: {
    // Правее — и кубок наполовину уезжает за край кадра: узкий телефон
    // прощает меньше, чем кажется по расчёту.
    rest: new THREE.Vector3(0.2, 0, 0.06),
    shake: new THREE.Vector3(0.07, 0.23, 0.12),
    pour: new THREE.Vector3(0.05, 0.2, 0.05),
  },
  rival: {
    // Справа, а не слева: слева стоит свеча, и кубок соперника вставал
    // ровно в неё — в кадре получалась одна тёмная банка с фитилём.
    // _Нашлось живой проверкой, по снимку глазами соперника._
    rest: new THREE.Vector3(0.19, 0, -0.34),
    shake: new THREE.Vector3(0.08, 0.22, -0.3),
    pour: new THREE.Vector3(0.04, 0.19, -0.16),
  },
};

/**
 * Свеча — единственный источник тепла в кадре, потому и слева, а не по
 * центру: центральный свет плоский и убивает тени.
 *
 * Ближе к центру, чем хотелось бы «по композиции»: при `x = −0,34` она
 * оказывалась за краем кадра, и в сцене был тёплый свет ниоткуда.
 */
export const CANDLE = new THREE.Vector3(-0.25, 0, -0.28);
