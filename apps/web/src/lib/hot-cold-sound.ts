import type { HotColdBand, SoundName } from '@bible-arena/shared';

/**
 * Лестница тепла на слух — одна на соло и на дуэль.
 *
 * Отдельных нот под «горячо» и «тепло» пока нет: взяты те, что уже есть, и
 * выстроены по светлоте. Находка звучит наградой, горячо — как верный
 * ответ, тепло — коротким высоким бликом, а холод и лёд остаются простым
 * щелчком. Своя лестница из четырёх нот была бы лучше, но она стоит
 * четырёх новых рецептов, и без них режим уже читается на слух.
 *
 * Лежит отдельным файлом, а не в экране, потому что режима два: таблица,
 * скопированная в дуэль, разошлась бы с соло на первой же правке.
 */
export const HOT_COLD_HEAT_SOUNDS: Record<HotColdBand, SoundName> = {
  FOUND: 'reward',
  HOT: 'correct',
  WARM: 'opponent',
  COLD: 'tap',
  ICE: 'tap',
};
