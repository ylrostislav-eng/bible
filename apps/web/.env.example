# Copy this file to .env.local in apps/web for local development.
#
# Only needed if the API isn't at its default address — the client falls
# back to http://localhost:3001 when this is unset.
NEXT_PUBLIC_API_URL="http://localhost:3001"
