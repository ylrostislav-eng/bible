import path from 'node:path';
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  turbopack: {
    root: path.join(__dirname, '../..'),
  },

  /**
   * Кто имеет право открывать dev-сервер не с localhost.
   *
   * Без этого списка Next отказывается отдавать внутренние файлы
   * `/_next/*` запросу, пришедшему с чужого источника, и приложение,
   * открытое через туннель, показывает пустой экран: разметка приходит,
   * скрипты — нет. Ошибка при этом видна только в консоли браузера, а на
   * экране просто ничего, поэтому симптом легко принять за поломку
   * приложения.
   *
   * Домены здесь — бесплатные туннели, через которые проверяется вход из
   * Telegram: `lhr.life` (localhost.run), `trycloudflare.com`,
   * `ngrok-free.app`. Адрес внутри домена выдаётся новый при каждом
   * запуске, поэтому перечислены поддомены целиком — иначе список
   * пришлось бы править по нескольку раз в день.
   *
   * На боевом сервере это не действует: настройка касается только
   * `next dev`, сборка отдаёт файлы кому угодно.
   */
  allowedDevOrigins: ['*.lhr.life', '*.trycloudflare.com', '*.ngrok-free.app'],
};

export default nextConfig;
