# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/

# Env
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# OS
.DS_Store

# Testing
coverage/

# Local Redis dump (created if redis-server is ever run outside Docker)
dump.rdb

# Промежуточные файлы сборки словаря смыслов: полгигабайта сырых векторов
# ConceptNet, из которых собирается apps/api/data/semantics-ru.bin. Сам
# словарь лежит в репозитории — собирать его на каждой машине незачем.
.cache/

# Временный файл, которым scripts/rerank-with-model.mjs спрашивает сервер о
# кандидатах. Он удаляется сам, но если прогон оборвать — останется лежать.
apps/api/.rerank-candidates.ts
