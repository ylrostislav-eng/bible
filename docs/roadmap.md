# Roadmap Bible Arena — постоянное указание и живой план

> Этот файл — не разовый отчёт. Это действующий мандат и рабочий чек-лист.
> Хранится в git, а не в переписке — переживает сжатие сессии, конец сессии,
> новую сессию, что угодно. Перед началом любой существенной работы над
> проектом стоит его перечитать.

## Мандат пользователя (сохранено дословно по смыслу, 2026-09-01)

Пользователь — не разработчик, делает такой проект первый раз. Он поручил:

- Этот аудит — **личный план работы**, по которому нужно двигаться и решать
  все проблемы. Терять его нельзя ни при каких обстоятельствах (сжатие
  сессии, новая сессия и т.д.) — именно поэтому он живёт здесь, в репозитории.
- Пользователь **доверяет принимать решения по своему усмотрению** — не нужно
  каждый раз спрашивать разрешения на то, что и так очевидно правильно
  сделать по этому списку.
- Планка качества: **не "нормально", а отлично** — всё должно быть продумано,
  предусмотрено, со всеми нужными фичами и возможностями.
- Цель — сделать приложение **успешным для всех возрастов** (см. персоны
  15/30/45/65 ниже), при необходимости добавляя новые фичи, режимы, игры —
  не ограничиваться только починкой того, что уже есть.
- Нужно **закрыть все дыры, баги и недочёты** из аудита.
- Нужно **регулярно самому проводить повторные аудиты** приложения — не
  дожидаться, пока пользователь укажет на проблему, а находить время
  проверять код и UX самостоятельно и по своей инициативе.

Практический вывод: этот файл — источник правды по приоритетам. Каждый раз,
когда что-то из списка ниже исправлено или сделано, отмечать чекбоксом и
коротко писать, что изменилось и в каком коммите. Каждый раз, когда проведён
новый самостоятельный аудит — дописывать новый раздел с датой, не затирая
историю прошлых находок.

---

## Аудит от 2026-09-01 (первый полный аудит)

Собран из: 4 параллельных проверок кода (комнаты/дуэли, друзья/чат/вход,
изучение/инфраструктура, интерфейс) + живого прохождения приложения на
экране телефона как обычный игрок. Полная версия с фильтрами и вёрсткой
опубликована как артефакт: https://claude.ai/code/artifact/58ec8862-c45f-4741-be77-5bcb3bb59ba7
(на случай, если сам артефакт когда-то станет недоступен — весь текст
находок продублирован ниже, это единственный надёжный источник).

### С чего начать (топ-10 приоритетов)

- [ ] Гонки при начислении опыта, рейтинга и монет — награда может теряться
      или удваиваться при почти одновременном завершении двух игр. Затрагивает
      дуэли, комнаты и «Изучение». (`users.service.ts`, `duel.service.ts`,
      `rooms.service.ts`, `learn.service.ts`)
- [ ] Двойное присоединение к одной дуэли по коду — третий участник может
      "застрять" со сломанной сессией. (`duel.service.ts`)
- [ ] Обрыв связи с Redis может уронить весь сервер целиком, не только статус
      "онлайн". (`redis.service.ts`)
- [ ] Дуэль может засчитаться завершённой дважды (опрос состояния + отправка
      ответа гонятся за одно и то же событие). (`duel.service.ts`)
- [ ] «Чёрный список» нельзя открыть ни с одного экрана интерфейса — экран
      существует и работает, ссылки на него нет нигде. (`/profile/blacklist`)
- [ ] Лимит длины сообщения в чате не проверяется на сервере, только в UI.
      (`chat.gateway.ts`)
- [ ] Два всплывающих полноэкранных приглашения (дуэль + комната) могут
      перекрыть друг друга — про одно из них можно не узнать вовсе.
      (`auth-gate.tsx`)
- [ ] Ошибки на экране меню комнат теряются — неудачный вход/принятие/отклонение
      ничего не показывает пользователю. (`play/room/page.tsx`)
- [ ] Заявки в друзья можно "сломать" гонкой при одновременных действиях с
      двух сторон. (`friends.service.ts`)
- [ ] Приглушённый серый текст слишком бледный по всему приложению — тяжело
      читать, особенно старшим пользователям и на солнце. (`globals.css`)

### Дуэли и комнаты (бэкенд)

**Критично**

- [ ] Опыт/рейтинг теряются или удваиваются при одновременных играх —
      начисление читает-считает-пишет без блокировки; монеты атомарны, опыт/
      уровень/рейтинг нет. `users.service.ts`
- [ ] Дуэль может завершиться и наградить игроков дважды — проверка
      "завершена?" и начисление не защищены от повторного запуска; проявляется
      почти в каждой игре с активным опросом на последнем вопросе. `duel.service.ts`
- [ ] Гонка при одновременном входе по коду дуэли ломает сессию второго
      участника, если код используют два человека почти одновременно.
      `duel.service.ts`

**Важно**

- [ ] Двойной клик по ответу может задвоить очки за вопрос при плохой связи
      (повторная отправка). `game/duel/rooms .service.ts`
- [ ] В комнату можно набрать больше игроков, чем лимит — проверка места и
      добавление участника не атомарны (в отличие от уже защищённой проверки
      уникальности названия). `rooms.service.ts`

**Средне**

- [ ] Чужую дуэль можно "подтолкнуть", просто зная её номер — побочные
      эффекты (пропуски, завершение, награда) выполняются до проверки участия.
      `duel.service.ts`
- [ ] WebSocket-действия в комнате не проходят валидацию, хотя схемы для
      проверки уже написаны, но не подключены. `rooms.gateway.ts`
- [ ] Комната зависает навсегда, если создатель так и не подключился ни разу
      (грейс-таймер починили только для случая, когда подключение уже было).
      `rooms.gateway.ts`
- [ ] Число вопросов может незаметно уменьшиться, если подходящих в базе
      меньше запрошенного — без предупреждения игрокам. `questions.service.ts`

**Мелочи**

- [ ] Отправленный вызов на дуэль нельзя отменить. `duel.controller.ts`
- [ ] Кик/бан уже вышедшего игрока даёт "сырую" ошибку вместо дружелюбной.
      `rooms.service.ts`
- [ ] Бан не убирает уже отправленное приглашение в ту же комнату (само
      исправляется при попытке принять). `rooms.service.ts`
- [ ] Коды приглашений можно перебирать без ограничения попыток. `invite-code.ts`

Хорошо сделано: блокировка уникальности названия комнаты через БД проверена
отдельно — гонок и взаимоблокировок не найдено; комментарии в коде честно
объясняют неочевидные решения.

### Друзья, чат, вход в приложение (бэкенд)

**Важно**

- [ ] Лимит длины сообщения в чате — только в UI, не на сервере. `chat.gateway.ts`

**Средне**

- [ ] Бейдж непрочитанных может залипнуть навсегда, если переписка стала
      недоступна (удаление из друзей) до прочтения. `chat.service.ts`
- [ ] Список чатов делает по одному запросу к БД на каждого друга — при
      сотнях друзей будет тормозить сервер для всех. `chat.service.ts`
- [ ] Заявки в друзья можно сломать гонкой (отправка/принятие/отклонение не
      атомарны). `friends.service.ts`

**Мелочи**

- [ ] Запись о дружбе не защищена на уровне БД с одной стороны — риск
      "мёртвых" записей при будущем удалении аккаунтов. `schema.prisma`
- [ ] Вход тестовым пользователем полагается только на режим сервера, а не
      явный флаг. `auth.service.ts`
- [ ] У чата более открытые сетевые настройки, чем у остального API. `chat.gateway.ts`
- [ ] Подгрузка старых сообщений может изредка пропустить/повторить одно при
      совпадении миллисекунды отправки. `chat.service.ts`
- [ ] Смена никнейма на занятый даёт "сырую" ошибку вместо понятной.
      `users.service.ts`

Хорошо сделано: переписка ограничена друзьями, бан закрывает её с обеих
сторон, дружба всегда хранится симметрично, Telegram-вход криптографически
верный, статус "в сети" аккуратно деградирует при недоступности Redis, прямых
способов прочитать чужие данные через подмену параметра не найдено.

### Изучение, Библия, инфраструктура (бэкенд)

**Важно**

- [ ] Обрыв связи с Redis может уронить весь сервер — ничто не обрабатывает
      сбой соединения. `redis.service.ts`
- [ ] Награду за проверку главы можно получить дважды (та же гонка, что и в
      дуэлях). `learn.service.ts`
- [ ] При гонке на последнем вопросе монеты дублируются, опыт может
      потеряться. `learn.service.ts`

**Средне**

- [ ] Таймер на вопрос в «Изучении» можно сбрасывать бесконечно повторными
      запросами. `learn.service.ts`
- [ ] Серия дней подряд (стрик) считается по UTC, а не по времени игрока —
      несправедливо для часовых поясов далеко от UTC. `users.service.ts`

**Мелочи**

- [ ] Пустой/слабый секретный ключ входа не блокируется при запуске сервера.
      `env.validation.ts`
- [ ] Нет ограничения на количество открываемых проверок глав. `learn.service.ts`

Хорошо сделано: health-check реально проверяет БД и Redis; авторизация везде
последовательно защищена.

### Интерфейс (веб-приложение)

**Важно**

- [ ] Два всплывающих приглашения (дуэль/комната) могут перекрыть друг друга.
      `auth-gate.tsx`
- [ ] Ошибки на экране меню комнат никак не показываются пользователю.
      `play/room/page.tsx`

**Средне**

- [ ] После перезагрузки в разгар игры на миг можно словить чужое
      приглашение, пока не подгрузилось реальное состояние (само исправляется
      за долю секунды). `play/duel, play/room page.tsx`
- [ ] При переключении переписки может остаться прокрутка сверху вместо
      автоскролла вниз. `chat-widget.tsx`
- [ ] Экран результата дуэли продолжает опрашивать сервер бесконечно после
      завершения — лишний расход батареи/трафика. `play/duel/page.tsx`
- [ ] Плавающие окна чата и приглашений могут перекрывать друг друга.
      `chat-widget.tsx / room-invites-widget.tsx`
- [ ] Приглушённый серый текст слишком бледный по всему приложению. `globals.css`

**Мелочи**

- [ ] Если не удалось обновить профиль после игры — пользователь не узнает,
      цифры молча остаются устаревшими. (несколько экранов `page.tsx`)
- [ ] Очень длинное название комнаты в лобби не обрезается многоточием.
      `play/room/page.tsx`
- [ ] Продублирован код загрузки чёрного списка. `profile/blacklist/page.tsx`
- [ ] Кнопка «Далее» в дуэли и автопереход могут сработать почти одновременно
      (безобидная ложная ошибка). `play/duel/page.tsx`
- [ ] Ползунок количества вопросов слишком узкий для пальца (8px). `question-count-slider.tsx`

Хорошо сделано: аккуратная защита от устаревших обновлений и очистка
таймеров/подписок почти в каждом эффекте; загрузка и ошибки продуманы почти
везде; комментарии честно объясняют историю прошлых багов.

### Общие наблюдения по всему приложению

- [ ] **«Чёрный список» полностью недостижим из интерфейса** — ни с профиля,
      ни с главной, ни с друзей; плюс редирект "всегда на главную" теперь
      блокирует и прямой заход по URL. Нужно решить, где ему место (скорее
      всего рядом с «Друзьями» или в «Настройках»), и добавить видимую ссылку.
- [ ] «Настройки» логичнее смотрелись бы в профиле, а не только плиткой на
      главной — по привычке их ищут в профиле.
- [ ] В «Изучении» не видно личного прогресса по главам (пройдено/осталось) —
      это обычно сильно мотивирует заниматься каждый день.
- [ ] «Турниры» пока просто заглушка «В разработке» — решить, показывать ли
      пункт меню уже сейчас или скрыть до готовности.

Хорошо и не нужно трогать: пустые состояния написаны по-человечески с
подсказкой следующего шага; приглашения на дуэль и в комнату ведут себя
единообразно; онбординг короткий с готовым предложенным ником.

### UX глазами четырёх персон

**15 лет (подросток, любит соревноваться и хвастаться)**

- Дуэли/комнаты/друзья/чат — то, что нужно, и уже работает.
- «Турниры» — просто заглушка, разочарование сильнее, чем для взрослых.
- «Достижения» пустые с самого начала без подсказки, что нужно сделать —
  не хватает быстрой победы в первые 5 минут.
- Короткий онбординг — плюс для нетерпеливой аудитории.

**30 лет (занятой взрослый, заходит урывками)**

- «Позже» на приглашениях + плавающая иконка вместо навязчивого окна — то,
  что нужно.
- Дуэль по коду и публичный список комнат — быстрый вход, это ценится.
- Заполнившаяся публичная комната молчит — непонятно, получилось зайти или
  нет.
- Настройки профиля короткие и по делу.

**45 лет (уверенный, но не гик)**

- «Настройки» на главном экране, а не в профиле — искать будут в профиле.
- Чёрный список вообще не виден в меню — для этого возраста важно чувствовать,
  что есть куда пожаловаться.
- Тёмная тема с золотым акцентом выглядит солидно, хорошо ложится на аудиторию.
- Мелкий приглушённый текст тяжеловат без очков.

**65 лет (старшее поколение, хорошо знает Библию, не всегда уверен с телефоном)**

- Главная сложность — контраст и размер мелкого текста, не логика приложения.
- Ползунок и некоторые значки без подписи — мелкая цель для пальца.
- Игровые термины («никнейм», «лобби», код приглашения) могут требовать
  пояснения.
- Суть (читать и вспоминать главы Библии) знакома и осмысленна — стоит
  подчёркивать это, а не прятать за игровой оболочкой.
- Возможность отложить решение («Позже», не пропадает) — хорошая уступка.

---

## Идеи развития (из мандата — "новые фичи, режимы, игры, если надо")

Пункты ниже — не баги, а куда можно расти, чтобы приложение было "супер, кайф"
для всех возрастов. Дополнять по мере появления новых идей и по итогам новых
аудитов.

- [ ] Полноценный раздел «Турниры» вместо заглушки (или временно скрыть пункт
      меню, если делать его нескоро).
- [ ] Прогресс по главам в «Изучении» (пройдено/осталось), возможно —
      визуальный трекер серии дней подряд.
- [ ] Первые «быстрые победы»/достижения в первые минуты для новых игроков
      (особенно важно для младшей аудитории).
- [ ] Видимая точка входа в «Чёрный список» — раздел «Друзья» или «Настройки».
- [ ] Переезд «Настроек» в профиль (или дублирование ссылки и там, и там).
- [ ] Пересмотр игровой терминологии на предмет понятности для старшей
      аудитории (подсказки/тултипы к «никнейм», «лобби», код приглашения).
- [ ] Общий контраст приглушённого текста по всему приложению — отдельная
      сквозная правка токенов, а не точечная.

---

## Журнал самостоятельных аудитов

Каждый раз, когда проводится новый самоинициированный аудит (по мандату —
"всегда находи время подумать над приложением, проводя всякие аудиты"),
добавлять сюда короткую запись: дата, что проверялось, что нашли нового,
ссылка на коммит/артефакт.

- **2026-09-01** — первый полный аудит (см. раздел выше). 82 находки в коде +
  живой UX-обход на телефоне. Ничего ещё не исправлено — план работы отсюда.
